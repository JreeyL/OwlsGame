CREATE TABLE IF NOT EXISTS tof_questions (
                                             id INT PRIMARY KEY,
                                             content VARCHAR(255),
    answer BOOLEAN
    );